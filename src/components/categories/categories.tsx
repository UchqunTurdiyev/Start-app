import React from 'react';
import SectionTitle from '../section-title/section-title';

const Categories = () => {
	return <SectionTitle title={'Top categories'} subtitle={'Learn our courses with high rating'} />;
};

export default Categories;
